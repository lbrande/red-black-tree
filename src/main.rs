fn main() {
    let mut thing = RefCell::new(Some(7));
    match &mut thing.borrow_mut() {
                    Some(thingy) => node.insert(value),
                    None => self.set_left_child(value),
                };
}